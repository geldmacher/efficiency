# Deploy local plugin

Read and follow [the repository deployment procedure](../../.agents/skills/deploy-local-plugin/SKILL.md). An explicit `/deploy-local-plugin` invocation selects the same workflow as `$deploy-local-plugin`; without a requested action, run the standard deployment described there.
