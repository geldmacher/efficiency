# Release Plugin

Read and follow [the repository release procedure](../../.agents/skills/release-plugin/SKILL.md). An explicit `/release-plugin` invocation authorizes the same complete release lifecycle described there for `$release-plugin`. Keep publication in its sole release script and preserve its checks and recovery boundaries.
